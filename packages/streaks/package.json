{
  "name": "streaks-app",
  "version": "1.0.0",
  "private": true,
  "description": "DZQL application for social habit tracking",
  "type": "module",
  "scripts": {
    "dev": "LOG_CATEGORIES=ws:debug,db:info,notify:debug bun --watch server/index.js",
    "test": "bun test tests/",
    "db": "bun db:down && bun db:up",
    "db:up": "cd database && docker compose -p streaks up -d",
    "db:down": "cd database && docker compose -p streaks down -v",
    "db:logs": "cd database && docker compose -p streaks logs postgres"
  },
  "dependencies": {
    "dzql": "workspace:*"
  }
}
