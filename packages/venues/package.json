{
  "name": "venues-app",
  "version": "1.0.0",
  "private": true,
  "description": "Example DZQL application for venue management",
  "type": "module",
  "scripts": {
    "dev": "bun --watch server/index.js",
    "build": "echo 'No build step required'",
    "test": "bun test tests/",
    "db": "bun db:down && bun db:up",
    "db:up": "cd database && docker compose -p venues up -d",
    "db:down": "cd database && docker compose -p venues down -v",
    "db:logs": "cd database && docker compose -p venues logs postgres"
  },
  "dependencies": {
    "dzql": "workspace:*"
  }
}
