services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: "{{name}}"
    ports:
      - "5432:5432"
    volumes:
      - ./generated/db/migrations:/docker-entrypoint-initdb.d:ro
