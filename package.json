{
  "name": "dzql-monorepo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "DZQL - PostgreSQL-powered framework with zero boilerplate",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "client": "cd packages/client && bun run dev",
    "server": "cd packages/rights && bun run dev",
    "dev": "concurrently -n \"client,server\" -c \"blue,green\" \"bun run client\" \"bun run server\"",
    "db": "cd packages/rights/database && docker compose down -v && docker compose up -d",
    "logs": "bun run --cwd packages/rights db:logs",
    "venues": "cd packages/venues && bun dev",
    "venues:test": "cd packages/venues && bun test",
    "venues:db": "cd packages/venues/database && docker compose down -v && docker compose up -d",
    "venues:logs": "bun run --cwd packages/venues db:logs"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "engines": {
    "bun": ">=1.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/your-org/dzql"
  },
  "keywords": [
    "postgresql",
    "websocket",
    "real-time",
    "crud",
    "api",
    "framework",
    "zero-boilerplate"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^9.2.1"
  }
}
